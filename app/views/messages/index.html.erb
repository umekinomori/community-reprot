<div class="content">
<%= render partial: "toppages/left-middle"%>
  <div class="chat-main">
    <div id="messages" class="messages">
    <%= render @messages %>
    </div>
  <div class="form">
    <% @message = Message.new unless @message %>
    <%= form_for @message do |f| %>
      <div class="new-message">
        <%=f.text_area :content, id:"chat-input", class:"input-text", placeholder:"type a message", "data-behavior" => "relationship_speak"   %>
        <%=f.submit '送信', id:"button", class:"submit-btn", name:"commit"  %>
      </div>
    <% end %>
     <!--<div class="form">
          <form class="new-message">
            <input id="chat-input"  class="input-text" placeholder="type a message" type="text" data-behavior="relationship_speak">
            <button id="button" class="submit-btn" type="submit" name="commit" value="Send">送信</button>
          </form>
        </div> -->
      </div>
    </div>
</div>